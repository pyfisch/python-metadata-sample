This repository contains `METADATA`, `WHEEL` and `PKG-INFO` collected from PyPI using a script.
The files should be used to write a new parser for the currently underspecified Python Metadata format.
